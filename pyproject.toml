[project]
name = "shemyu"
version = "0.1.0"
description = "Generate a GitHub profile and resumes from structured career data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-generativeai>=0.8.5",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0.3",
]
